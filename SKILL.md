---
name: form-filler
description: 智能填表（智填 FormFiller）。当用户要求填表、填充 Word/Excel 模板、批量套打、把简历或资料里的信息填进登记表/报价单/申请表时使用。支持两类模板：{{字段名}} 占位符模板，以及「标签格+空格」的传统表格型表单（自动识别、自动切换）。从资料文件（docx/xlsx/pdf/txt/md/csv/json/图片，含 .docm）中提取键值信息，经别名智能匹配后原位填充，多行列表（学习经历/工作经历/论文/亲属等）按表头自动映射并扩展行数，保留模板原有字体、颜色、表格样式，支持 {{img:图片名}} 插入图片。
agent_created: true
---

# 智填 FormFiller

从资料文件提取信息 → 智能匹配 → 填充 Word/Excel 模板 → 输出格式一致的文件。

**核心特点**：纯规则实现（别名表 + 后缀匹配），不调用任何大模型 API，不联网，可离线运行。

## 环境

```
Python:  <本机 Python 解释器>（含 python-docx / openpyxl / pdfplumber / FastAPI / uvicorn）
项目目录: ~/.workbuddy/skills/form-filler
```

## 两种模式，怎么选

| 场景 | 用哪个 | 理由 |
|------|--------|------|
| 一次性：把 A 文件的信息填进 B 模板 | **CLI**（`fill`） | 一条命令出结果，不用起服务 |
| 反复用：同一批个人/单位信息填多种表格 | **Web 界面** | 资料库会持久化在 `data/db.json`，建一次反复用 |
| 模板里有图片占位符 `{{img:头像}}` | 两者都行 | 图片按文件名自动匹配 |

**资料库是这个工具的核心价值**——如果用户的资料会被反复使用（比如填各种申请表、登记表），优先推荐 Web 模式，把资料先建起来。

## 命令行用法

**第一步——看模板有哪些占位符**：

```bash
cd ~/.workbuddy/skills/form-filler
<PYTHON> cli.py scan --template <模板.docx>
```

**第二步——填充**：

```bash
<PYTHON> cli.py fill \
  --template <模板.docx> \
  --source <简历.docx> <岗位信息.txt> <头像.png> \
  --out <输出文件.docx>
```

缺的字段用 `--set` 补，可重复传：

```bash
<PYTHON> cli.py fill --template tpl.docx --source 简历.docx \
  --set 签名=李云飞 --set 签署日期=2026-08-30 --out 结果.docx
```

有缺失字段时默认**拒绝生成并列出缺什么**（避免生成残缺文件）。确认可以留空再加 `--force`。

**命名规则**：`--name "{{姓名}}_入职登记表_{{当前日期}}.docx"`

**启动网页版**：

```bash
<PYTHON> cli.py serve --port 8765
```

访问 http://127.0.0.1:8765

## Web 模式启动

```bash
cd ~/.workbuddy/skills/form-filler/backend
<PYTHON> -m uvicorn main:app --host 127.0.0.1 --port 8765
```

注意：**必须在 `backend/` 目录下执行**。后端用的是扁平导入（`import extractor`），不是包相对导入，从别处启动会报 ModuleNotFoundError。

## 上传报 500 怎么办

**首要排查：`data/` 目录还在不在。**

`data/` 存放资料库、模板、生成结果。若它在服务运行期间被删掉
（清理脚本、手动整理、杀毒软件），上传会抛 `FileNotFoundError`，
FastAPI 默认只回一句 `Internal Server Error`，界面上看不出任何线索。

已做两处修复：

1. `storage.py` 新增 `ensure_dirs()`，`_save()` 与 `save_upload()` 每次写入前都兜一次，
   目录缺失会**自动重建**（回归测试：运行中删除 data/ 后上传仍返回 200）。
2. `main.py` 加了全局异常处理，未捕获的异常会回传
   `{"detail": "异常类型: 信息"}`，不再是无信息的 500。

如果仍报错，返回的 `detail` 会写清楚原因，照着修即可。

⚠️ **不要在服务运行时手工删除 `data/`**——即便现在能自愈，已上传的文件记录还在
`data/db.json` 里，删掉目录会让元数据与实际文件对不上。要清空请在 Web 界面里逐项删除。

## 占位符语法

| 类型 | 写法 | 说明 |
|------|------|------|
| 文本 | `{{姓名}}`、`{{电话}}` | 从资料库取值填充 |
| 图片 | `{{img:头像}}` | 插入资料库中的图片 |
| 命名 | `{{姓名}}_入职登记表_{{当前日期}}` | 输出文件名模板 |
| 动态 | `{{当前日期}}`、`{{当前时间}}` | 自动填系统时间 |

## 表格型表单模式（form mode）

机关/高校的正式表格（申报书、应聘人员信息表等）通常**没有 `{{占位符}}**，
而是「标签格 + 空格」结构，如 `姓名 | (空)`。工具会自动处理：

1. `cli.py fill` 先按占位符模式解析；解析到 **0 个占位符**时自动切换到表格型表单模式
   （提示语：`模板没有 {{占位符}}，已自动切换到「表格型表单」模式`）。
2. **模板分析**：`form_analyzer.analyze_template_fields()` 扫描所有表格，
   找出「一个非空标签格 + 相邻空格」的字段位；自动跳过合并单元格的重复项和
   `XXX大学`、`X年X月至X年X月` 这类示例占位文本。
3. **资料索引**（双路径）：对每份资料同时走两条提取路径后合并——
   - 表格路径 `build_source_index()`：读「标签格 + 值格」的表格型资料（如 .docm 应聘信息表）
   - 文本路径 `extract_kv_from_text()`：读「标签：值」文本，**含文本框（textbox）里的正文**
     （很多简历/申报材料把正文放在文本框里，python-docx 的 paragraphs/tables 读不到，
     已从 XML 层补抓），也覆盖 txt/pdf/xlsx
   再按别名表 + 字符重叠相似度匹配。
4. **派生字段**：按月份精算年龄（不是减出生年）；复合标签如「最后学历毕业院校及学位」
   会从多个源字段拼接（院校 + 学位）。
5. **列表区块**：`analyze_list_tables()` 识别「表头 + 数据行」结构的多行表
   （学习经历/工作经历/已发表论文/家庭成员等），按表头列名相似度做列映射，
   数据行数不够时自动加行（`append_rows=True`）。
6. **论文引用解析**（`parse_citation` / `extract_papers_from_text`）：简历里的
   「发表论文」常是文本段落（甚至在文本框里），列表区块的表格路径读不到。
   工具会从全文文本解析 APA 引用，拆出作者列表→作者排序、标题、期刊、ISSN、
   影响因子、收录情况、年卷期页、引用次数，并内置常见期刊的 ISSN 映射表。
   - 作者排序靠「本人」定位：用 `--set 本人=<英文姓氏/拼音>`（如 `--set 本人=Wang`）指定，
     工具据作者列表位置推导「第一/第二/第三作者」；不指定则留空。
   - 是否通讯作者：仅当文本出现「通讯作者」字样或星号才判「是」，否则留空待确认。
7. 禁用方式：`--no-form-mode` 强制占位符模式，`--no-lists` 跳过列表填充。

实测（高校申报书模板 + 应聘人员信息表.docm + 个人简历.docx 三份资料）：
19 个字段 + 4 张列表表全部正确填入（含家庭地址、通讯地址由简历「住址」补上），
4 个资料中确实没有的字段（申报类别、户籍所在地、档案所在地、计划入职时间）正确留空待补。
论文表用 `--set 本人=<本人英文姓氏>` 可自动填全：作者排序（第三/第一作者）、ISSN、影响因子、
收录情况、年卷期页均由简历文本解析得出。

## 智能匹配规则

资料库字段按以下顺序匹配占位符，**不需要名字完全一致**：

1. **别名表**：`联系电话`→`电话`、`电子邮箱`→`邮箱`、`毕业院校`→`学校`、`学号/工号`→`编号`
2. **后缀匹配**：`现居地址`→`地址`、`入职部门`→`部门`
3. **图片序号兼容**：`头像_1.png`、`头像(2).png` 都能匹配 `{{img:头像}}`

实测效果（内置样例）：16 个占位符中 14 个自动匹配，其中 3 处靠智能匹配命中。

## 支持格式

- **模板**：`.docx`、`.xlsx`（表格型表单模式仅支持 .docx）
- **资料文件**：docx、**docm**（含宏的 Word，读取时在内存中转正 content type）、
  xlsx、pdf、txt、md、csv、json 及常见图片
- **文本框简历**：正文在文本框（textbox）里的 docx 也能正常提取（从 XML 层抓取）
- **`.doc` 旧格式**：提取器不直接读 `.doc`（OLE2 二进制）。Windows 上有 Word 时，
  先用 Word COM 转成 `.docx` 再喂给工具：
  `Documents.Open(..., ReadOnly).SaveAs2(dst, 16)`（16 = wdFormatDocumentDefault）

## 工作流程

1. **先 scan 看模板结构**，确认有哪些占位符、其中哪些资料里能提供。
   若 scan 显示 0 个占位符，说明是表格型表单，直接 fill（会自动切换，见上文）。
2. **再 fill**。若报缺失，把字段名告诉用户并询问取值——不要擅自 `--force` 留空后当作成功交付。
3. **生成后必须交付文件**，用 present_files 呈现。
4. **提醒人工复核**，尤其是签名、日期、金额这类字段。

## 注意事项

- **不要上报"生成成功"却不交付文件**。
- 复杂图表、宏、特殊控件可能因 python-docx / openpyxl 的限制无法保留，涉及这类模板时提前说明。
- 公司名等字段可能填在**页眉/页脚**里，校验时要连页眉一起扫，只看 `document.paragraphs` 会漏。
- 原云端版本有 6 处硬编码 Linux 路径（`/workspace/form-filler/...`、`/tmp/ff_test`），全部在测试脚本与样例生成脚本里，已改为相对定位。
- 模板与生成结果保存在 `data/` 目录（首次运行自动创建），资料库在 `data/db.json`。
- 内置样例在 `sample/`（员工入职登记表模板、产品报价单模板、李云飞个人简历、岗位信息、头像），可用来验证环境。
